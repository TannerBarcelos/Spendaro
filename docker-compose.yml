version: '3.8'
services:
  rest-service:
    build:
      context: ./spendaro/api
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
  client-service:
    build: 
      context: ./spendaro/client
      dockerfile: Dockerfile
      target: dev
    ports:
      - "5173:5173"
